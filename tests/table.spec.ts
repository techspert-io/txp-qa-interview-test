import { test } from '@playwright/test';

test.fixme('details of all individuals are displayed', async ({ page }) => {
  await page.goto('/table');
});

test.fixme('can view the full details of an individual', async ({ page }) => {
  await page.goto('/table');
});
